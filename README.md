# 平均打字速度記錄器 v0.1

Chrome Extension（Manifest V3 + TypeScript + Vite + React），用於記錄平均打字速度。第一版只統計字元數、時間、WPM、CPM，不保存任何實際輸入文字內容。

## 開發

```bash
npm install
npm run build
```

建置後至 Chrome 擴充功能頁面載入 `dist` 目錄。
